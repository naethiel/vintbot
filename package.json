{
  "private": true,
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev:web": "npm run dev -w @vintbot/web",
    "dev:service": "npm run dev -w @vintbot/service",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^2.8.7"
  },
  "name": "vintbot-mono",
  "packageManager": "npm@8.1.1",
  "workspaces": [
    "packages/*"
  ]
}
